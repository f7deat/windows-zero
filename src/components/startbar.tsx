export default function StartBar(props: any) {

    return (
        <div className="w-64 bg-gray-800 transition duration-700 overflow-hidden" style={{height: props.startBarHeight}}>
            
        </div>
    )
}